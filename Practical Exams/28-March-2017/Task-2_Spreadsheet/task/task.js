function solve() {

    return function(selector, rows, columns) {

        var $selector = $(selector);
        var $$table = $('<table>').appendTo($selector);


    };
}

// SUBMIT THE CODE ABOVE THIS LINE

if (typeof module !== 'undefined') {
    module.exports = solve;
}